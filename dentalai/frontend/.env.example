# Copy to .env before running `npm run dev`

# URL of the FastAPI backend (no trailing slash)
VITE_API_URL=http://localhost:8000

# Optional: pre-populate the Bearer token so you skip the token gate UI.
# The backend prints this on startup. Leave blank to use the in-app token gate.
VITE_DEV_TOKEN=
